# Claude-Baseline 05

```text
chore(plugin): Manifest-Versionen auf striktes SemVer migrieren
```

**Begründung:** `type=chore` (Wartung, keine Verhaltensänderung), `scope=plugin` (4 Manifest-JSONs), Imperativ „migrieren", 63 Zeichen.
