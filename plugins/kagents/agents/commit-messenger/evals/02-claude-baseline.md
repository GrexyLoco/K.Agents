# Claude-Baseline 02

```text
fix(ci): Release-Asset-Upload auf Stable-Tags beschränken
```

**Begründung:** `type=fix` (verhindert „release not found" bei Pre-Release-Tags), `scope=ci` (Workflow), Imperativ „beschränken", 57 Zeichen.
