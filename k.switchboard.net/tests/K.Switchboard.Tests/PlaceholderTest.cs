namespace K.Switchboard.Tests;

/// <summary>Platzhalter — wird in Phase 2 durch echte Tests ersetzt.</summary>
public sealed class PlaceholderTest
{
    [Test]
    public async Task Placeholder_Passes()
    {
        var value = 1 + 1;
        await Assert.That(value).IsEqualTo(2);
    }
}
